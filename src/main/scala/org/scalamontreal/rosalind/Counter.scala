package org.scalamontreal.rosalind

object Counter {

  /**
   * Setup for [[http://rosalind.info/problems/dna/]]
   *
   * FURTHER EXPLORATION:
   *
   * How could we design the Counter so that we could count the nucleotides of many DNA string?
   * What if these DNS strings come at different time and we need to accumulate the counts?
   *
   * @param dna dna String
   * @return four integers counting the respective number of times that the symbols 'A', 'C', 'G', and 'T' occur in s.
   */
  def countNucleotides(dna: String): (Int, Int, Int, Int) = {
    ???
  }

}
